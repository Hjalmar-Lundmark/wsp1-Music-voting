{% extends "layout.njk" %}
{% block  content %}
<h1>{{ title }}</h1>

{% if error.err[0] %}
<div class="error">
    <ul>
        <h3>Errors</h3>
        {% for row in error.err %}
        <li>
            {{ row }}
        </li>
        {% endfor %}
    </ul>
</div>
{% endif %}

<form action="/register" method="POST">
    <label for="title">Användarnamn:</label>
    <input type="text" name="username" id="username">
    <label for="title">Lösenord:</label>
    <input type="password" name="password" id="password">
    <label for="title">Lösenord bekräftelse:</label>
    <input type="password" name="passwordConfirmation" id="password">
    <br>
    <button type="submit">Skapa konto</button>
</form>

{% endblock %}